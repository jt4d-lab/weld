# Концепции

В основе подхода лежат простые концепции:

## Публичный API директории

`index` в директории явно указывает публичный API этой директории

<a href="./public-api.html">
  <Badge text="Подробнее →" type="tip" />
</a>

## Однонаправленность зависимостей

Слой импортирует только из слоёв ниже себя

## **Слои задаёт проект**

Методология даёт форму, а набор и порядок слоев выбирает проект

## **Общие слои и фрактальность модулей**

Общие слои наверху, модули в `modules/` — со своими слоями и своими вложенными `modules/`
