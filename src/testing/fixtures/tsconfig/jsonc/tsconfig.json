{
    // Комментарий: фикстура проверяет разбор JSONC.
    "compilerOptions": {
        "baseUrl": ".", // строчный комментарий после значения
        /* блочный комментарий */
        "paths": {
            "~/*": ["app/*"],
        },
    },
}
